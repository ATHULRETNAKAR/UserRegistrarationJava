spring.application.name=auth-backend
# Disable Spring AI completely
spring.ai.enabled=false

# Exclude MongoDB Vector Store Auto Config
spring.autoconfigure.exclude=org.springframework.ai.vectorstore.mongodb.autoconfigure.MongoDBAtlasVectorStoreAutoConfiguration
